# KTVS Enterprise - Production Requirements
# Python 3.11+

# Django and core
Django>=5.2.9,<6.0
python-dotenv>=1.0.0

# Database
pymongo>=4.6.0
psycopg2-binary>=2.9.9  # PostgreSQL adapter

# Caching
django-redis>=5.4.0
redis>=5.0.0

# WSGI Server
gunicorn>=21.2.0
whitenoise>=6.6.0  # Static file serving

# Security
cryptography>=41.0.0

# OAuth (optional - comment out if not using)
social-auth-app-django>=5.4.0
social-auth-core>=4.5.0

# QR Code generation
qrcode>=7.4.2
Pillow>=10.2.0

# Monitoring (optional)
sentry-sdk>=1.40.0

# Email
# No additional packages needed - using Django's built-in

# Production helpers
python-decouple>=3.8  # Alternative to python-dotenv
